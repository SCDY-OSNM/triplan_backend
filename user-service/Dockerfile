FROM openjdk:17-jdk-slim
WORKDIR /app

# 환경변수 정의
ARG REDIS_URL
ARG REDIS_PORT
ARG TRIPLAN_EUREKA_URL
ARG TRIPLAN_MYSQL_URL
ARG MYSQL_USER
ARG MYSQL_PASSWORD
ARG JWT_KEY

# 환경변수 설정
ENV REDIS_URL=${REDIS_URL}
ENV REDIS_PORT=${REDIS_PORT}
ENV TRIPLAN_EUREKA_URL=${TRIPLAN_EUREKA_URL}
ENV TRIPLAN_MYSQL_URL=${TRIPLAN_MYSQL_URL}
ENV MYSQL_USER=${MYSQL_USER}
ENV MYSQL_PASSWORD=${MYSQL_PASSWORD}
ENV JWT_KEY=${JWT_KEY}

COPY build/libs/user-service-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 38081

ENTRYPOINT ["java", "-jar", "app.jar"]
